name: Test and Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install Backend Dependencies
        run: |
          cd backend
          pip install -r requirements.txt
      
      - name: Install Frontend Dependencies
        run: |
          cd frontend
          npm ci
      
      - name: Test Backend
        run: |
          cd backend
          python -c "import app; print('✅ Backend imports successfully')"
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
      
      - name: Test Frontend Build
        run: |
          cd frontend
          npm run build
      
      - name: Deploy Status
        run: |
          echo "✅ All tests passed!"
          echo "🚀 Ready for manual deployment to Vercel"
          echo "📋 Backend: https://vercel.com/dashboard (backend directory)"
          echo "📋 Frontend: https://vercel.com/dashboard (frontend directory)" 